export class User {
  _id!: string;
  firstName!: string; 
  lastName!: string;
  userName!: string;
  email!: string;
  profileImage!: string;
  // Optionally include isAdmin, role, createdAt, etc. if needed
}